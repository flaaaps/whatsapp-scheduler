# Authentication credentials for web UI
ADMIN_USER=admin
ADMIN_PASS=password

# WhatsApp connection name
WHATSAPP_CONNECTION_NAME=Dev_User

# Server port (default: 3000)
PORT=3000

# Node environment
NODE_ENV=production

# PostgreSQL Database Configuration
POSTGRES_USER=whatsapp
POSTGRES_PASSWORD=whatsapp123
POSTGRES_DB=whatsapp_scheduler

# Database connection (for app)
DB_HOST=localhost
DB_PORT=5432
DB_USER=whatsapp
DB_PASSWORD=whatsapp123
DB_NAME=whatsapp_scheduler
